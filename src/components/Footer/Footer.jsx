import styles from './Footer.module.scss';

const Footer = ({}) => {
	return (
		<footer className={styles.footer}>
			<p className={styles.titulo}> By. SirReinato</p>
 		</footer>
	);
};


export default Footer;